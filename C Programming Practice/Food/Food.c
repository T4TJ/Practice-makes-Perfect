#include <stdio.h>

int main()
{
    char i;
    char j;
    char k;

    if (i < 0)
        ;
    (i > 122);
    (i++);
    {
        printf("Enter your Breakfast:");
        scanf("%c \n", &i);
    }
    return 0;

    if (j < 0)
        ;
    (j > 122);
    (j++);
    {
        printf("Enter your Lunch:");
        scanf("%c \n", &j);
    }
    return 0;

    if (k < 0)
        ;
    (k > 122);
    (k++);
    {
        printf("Enter your Dinner:");
        scanf("%c \n", &i);
    }
    return 0;
}
