#include <stdio.h>

int main()
{
    int i;
    printf("Enter five digit of your choice:");
    scanf("%d", &i);

    if (i > 0; i = 10; i--)
    {
        printf("\n");
        printf("The reverse of the number you entered is %d", i);
    }
    return (0);
}
